package Group2;

public class Formulas {
//    int tripDays = (dateto - datefrom)
    // double travePrice
/*     if (tripDays < 5) {
         travePrice = (generalprice * adults) + (childrenprice * childrens) + (tripDays* generalprice)
    } esle {
        travePrice = (generalprice * adults) + (childrenprice * childrens) + (tripDays* bulkprice)
    }*/
    //MAIN LAPA
    // System.out.println("Do you have CovidPass? y/n");
    // covidPassInput = scanner.next().charAt(0);
//if (covidPassInput = covidpass) {
    //System.out.println("You are allowed to travel to" + inputCountry)
    // } else {
    //System.out.println("Sorry, but without CovidPAss you are not able travel to" + inputCountry)
//    }
}
